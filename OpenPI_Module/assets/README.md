# OpenPI assets

For final packaging, place `paligemma_tokenizer.model` in this directory or update
`configs/openpi_comp_action_chunk_runtime.yaml` to point at the packaged tokenizer.

During local development the config points at the tokenizer already present in the
old `RO-Inference/OpenPI_Module/assets` directory.
