# Checkpoint Planner Weights

Place the trained checkpoint-planner `.pt` file for `train_checkpoint_planner_224_headleft_tactile_distill` under this directory, or point `configs/dataset_replay.yaml` to the exact checkpoint path.
